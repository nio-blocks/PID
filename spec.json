{
  "nio/PID": {
    "version": "0.1.2",
    "description": "Creates a discretized PID Controlled output from a list of incoming signals.",
    "categories": [
      "Signal Inspection",
      "Machine Learning"
    ],
    "properties": {
      "gain_config": {
        "title": "Gain Values",
        "type": "ObjectType",
        "description": "Configurable gains of the PID Controller.",
        "default": 0
      },
      "process_config": {
        "title": "Process Variable Setup",
        "type": "ObjectType",
        "description": "Process Variable and Set Point for the controller.",
        "default": 0
      }
    },
    "inputs": {
      "default": {
        "description": "Any list of signals."
      }
    },
    "outputs": {
      "value": {
        "description": "Calculated control output from the PID controller."
      }
    },
    "commands": {}
  }
}
